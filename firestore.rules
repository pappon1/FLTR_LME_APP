rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ EMERGENCY DEBUG MODE: Allowing everyone to read and write temporarily
    // to diagnose if the issue is App Check or Auth State.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}